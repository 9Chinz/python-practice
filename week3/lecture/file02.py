#---------------------------------------------------
#*การค้นหา string 
#* k = s.find(c) คืน index น้อยสุดที่พบ c ใน s 
#* เริ่มต้นตั้งแต่ index 0 ถ้าไม่พบ จะได้ผลเป็น -1
#---------------------------------------------------

k = "engineering".find("ng")
print(k)

#* if c in s เหมือนกับ if s.find(c) >= 0

if "engineering".find("ng") >= 0:
    print("found")